server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Single-Page App routing: for any path that isn't a real file, serve
    # index.html so React Router / the client app can handle the route.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
